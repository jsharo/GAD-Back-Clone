/**
 * request-type.enum.ts — Request type enum.
 */

export enum RequestType {
  CONSTRUCTION_PERMIT = 'CONSTRUCTION_PERMIT',
  BUILDING_LINE       = 'BUILDING_LINE',
  PLAN_APPROVAL       = 'PLAN_APPROVAL',
}
